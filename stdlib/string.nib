func _chars_match_at(haystack, needle, at) {
    let nLen = needle.len();
    if at + nLen > haystack.len() {
        return false;
    }
    let j = 0;
    while j < nLen {
        if haystack[at + j] != needle[j] {
            return false;
        }
        j = j + 1;
    }
    return true;
}

func str_index_of(s, needle) {
    let sChars = s.chars();
    let nChars = needle.chars();
    let nLen = nChars.len();
    if nLen == 0 {
        return 0;
    }
    let i = 0;
    while i <= sChars.len() - nLen {
        if _chars_match_at(sChars, nChars, i) {
            return i;
        }
        i = i + 1;
    }
    return -1;
}

func str_contains(s, needle) {
    return str_index_of(s, needle) >= 0;
}

func str_starts_with(s, prefix) {
    return _chars_match_at(s.chars(), prefix.chars(), 0);
}

func str_ends_with(s, suffix) {
    let sChars = s.chars();
    let suChars = suffix.chars();
    if suChars.len() > sChars.len() {
        return false;
    }
    return _chars_match_at(sChars, suChars, sChars.len() - suChars.len());
}

func str_slice(s, start, end) {
    let chars = s.chars();
    let result = "";
    let i = start;
    while i < end {
        result = result + chars[i];
        i = i + 1;
    }
    return result;
}

func str_repeat(s, count) {
    let result = "";
    let i = 0;
    while i < count {
        result = result + s;
        i = i + 1;
    }
    return result;
}

func str_split(s, sep) {
    let sChars = s.chars();
    let sepChars = sep.chars();
    let sepLen = sepChars.len();
    let sLen = sChars.len();
    if sepLen == 0 {
        return sChars;
    }
    let result = [];
    let current = "";
    let i = 0;
    while i < sLen {
        if _chars_match_at(sChars, sepChars, i) {
            result.push(current);
            current = "";
            i = i + sepLen;
        } else {
            current = current + sChars[i];
            i = i + 1;
        }
    }
    result.push(current);
    return result;
}

func str_replace(s, search, replacement) {
    let sChars = s.chars();
    let seChars = search.chars();
    let seLen = seChars.len();
    let sLen = sChars.len();
    if seLen == 0 {
        return s;
    }
    let result = "";
    let i = 0;
    while i < sLen {
        if _chars_match_at(sChars, seChars, i) {
            result = result + replacement;
            i = i + seLen;
        } else {
            result = result + sChars[i];
            i = i + 1;
        }
    }
    return result;
}
