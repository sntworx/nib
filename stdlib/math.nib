func math_abs(x) {
    if x < 0 {
        return -x;
    }
    return x;
}

func math_min(a, b) {
    if a < b {
        return a;
    }
    return b;
}

func math_max(a, b) {
    if a > b {
        return a;
    }
    return b;
}

func math_clamp(x, lo, hi) {
    if x < lo {
        return lo;
    }
    if x > hi {
        return hi;
    }
    return x;
}

func math_pow(base, exponent) {
    if exponent < 0 {
        return 1.0 / math_pow(base, -exponent);
    }
    var result = 1;
    var i = 0;
    while i < exponent {
        result = result * base;
        i = i + 1;
    }
    return result;
}
