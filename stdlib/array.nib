func array_contains(arr, value) {
    for item in arr {
        if item == value {
            return true;
        }
    }
    return false;
}

func array_index_of(arr, value) {
    let i = 0;
    for item in arr {
        if item == value {
            return i;
        }
        i = i + 1;
    }
    return -1;
}

func array_join(arr, sep) {
    let result = "";
    let i = 0;
    for item in arr {
        if i > 0 {
            result = result + sep;
        }
        result = result + item;
        i = i + 1;
    }
    return result;
}

func array_reverse(arr) {
    let result = [];
    let i = arr.len() - 1;
    while i >= 0 {
        result.push(arr[i]);
        i = i - 1;
    }
    return result;
}

func array_slice(arr, start, end) {
    let result = [];
    let i = start;
    while i < end {
        result.push(arr[i]);
        i = i + 1;
    }
    return result;
}

func array_sum(arr) {
    let total = 0;
    for item in arr {
        total = total + item;
    }
    return total;
}

func array_min(arr) {
    let m = arr[0];
    for item in arr {
        if item < m {
            m = item;
        }
    }
    return m;
}

func array_max(arr) {
    let m = arr[0];
    for item in arr {
        if item > m {
            m = item;
        }
    }
    return m;
}

func array_map(arr, fn) {
    let result = [];
    for item in arr {
        result.push(fn(item));
    }
    return result;
}

func array_filter(arr, fn) {
    let result = [];
    for item in arr {
        if fn(item) {
            result.push(item);
        }
    }
    return result;
}

func array_reduce(arr, fn, initial) {
    let acc = initial;
    for item in arr {
        acc = fn(acc, item);
    }
    return acc;
}
